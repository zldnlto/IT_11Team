/**
 * 원의 넓이는 반지름의 길이 x 반지름의 길이 x 3.14 로 구한다.
 * 함수를 사용하여 원의 넓이를 구하는 코드를 작성하라.
 * 
 * 입력 값으로는 반지름의 길이 n 이 주어진다.
 * 반환값은, 원의 넓이.
 */

function area(n){
  let a = n * n * 3.14;
  console.log(a);
}

area(3);